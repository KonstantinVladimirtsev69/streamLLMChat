---
phase: 06-next-js-frontend-interface
plan: 04
wave: 4
depends_on:
  - "06-03"
requirements:
  - R5
  - R6
  - UI-05
files_modified:
  - frontend/src/components/header/BalanceChip.tsx
  - frontend/src/components/chat/ZeroBalanceAlert.tsx
  - frontend/src/components/modals/ReferralModal.tsx
  - frontend/src/app/chat/page.tsx
  - frontend/src/components/header/Header.tsx
  - frontend/src/components/chat/ChatInput.tsx
autonomous: true
must_haves:
  truths:
    - "D-05: Виджет баланса в шапке отображает актуальную сумму в рублях с автоматическим локальным списанием после каждого ответа модели"
    - "D-01: При нулевом балансе (<= 0.00 ₽) или ошибке 402 поле ввода блокируется с выводом предупреждающего баннера"
    - "D-01: Модальное окно реферальной программы позволяет скопировать ссылку с кодом пользователя и отображает заработанные бонусы"
    - "Сборка production bundle (npm run build) и проверка типов tsc проходят со 100% успехом без ошибок"
  artifacts:
    - path: "frontend/src/components/header/BalanceChip.tsx"
      provides: "Виджет баланса в рублях с открытием реферального окна"
      contains: "BalanceChip"
    - path: "frontend/src/components/chat/ZeroBalanceAlert.tsx"
      provides: "Баннер блокировки ввода при исчерпании баланса"
      contains: "ZeroBalanceAlert"
    - path: "frontend/src/components/modals/ReferralModal.tsx"
      provides: "Модальное окно реферальной программы и начислений"
      contains: "ReferralModal"
  key_links:
    - from: "frontend/src/components/header/BalanceChip.tsx"
      to: "frontend/src/components/modals/ReferralModal.tsx"
      via: "setReferralModalOpen"
      pattern: "setReferralModalOpen"
    - from: "frontend/src/components/chat/ChatInput.tsx"
      to: "frontend/src/components/chat/ZeroBalanceAlert.tsx"
      via: "balance <= 0 lockout condition"
      pattern: "isZeroBalance"
  prohibitions:
    - statement: "MUST NOT allow sending messages when balance is <= 0.00 RUB"
      status: resolved
      verification: test
    - statement: "MUST NOT hardcode referral bonus amounts in UI (use formatRub and store data)"
      status: resolved
      verification: code_review
---

<objective>
Реализовать систему отображения баланса, блокировку ввода при исчерпании средств и модальное окно реферальной программы (R5, R6, UI-05): интерактивный чип `BalanceChip` в шапке с форматированием `0.00 ₽` и мгновенным списанием по `cost_kopecks`, баннер блокировки ввода `ZeroBalanceAlert` при балансе `<= 0.00 ₽` или ошибке 402, модальное окно `ReferralModal` с копированием реферальной ссылки в буфер обмена и статистикой начислений (+2.00 ₽ за друга), и провести финальную валидацию сборки (`npm run build`).
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-next-js-frontend-interface/06-SPEC.md
@.planning/phases/06-next-js-frontend-interface/06-CONTEXT.md
@.planning/phases/06-next-js-frontend-interface/06-RESEARCH.md
@.planning/phases/06-next-js-frontend-interface/06-VALIDATION.md
@.planning/sketches/001-chat-workspace/index.html
</context>

<tasks>

<task id="06-04-01" wave="4">
<title>Виджет баланса BalanceChip и моментальное списание по токенам</title>
<action>
1. Создать `frontend/src/components/header/BalanceChip.tsx`:
   - Считывает `user` из `useChatStore`.
   - Рендерит стилизованный интерактивный чип: иконка `Wallet`, сумма в формате `formatRub(user?.balance_rub ?? 0)` (например, `5.00 ₽`).
   - Если баланс `<= 0.00 ₽`, чип выделяется янтарным/красным предупреждающим цветом с иконкой `AlertCircle`.
   - Клик по чипу вызывает `setReferralModalOpen(true)` для быстрого перехода к получению бонусов.
2. Встроить `BalanceChip` в `frontend/src/components/header/Header.tsx` правее селектора моделей.
3. В хуке `useChatStream.ts` при получении SSE-события `done`:
   - Вызывать `applyDeduction(event.usage.cost_kopecks)` в сторе.
   - Запускать фоновый `apiFetch('/api/v1/auth/me')` для асинхронной сверки точного баланса с базой данных PostgreSQL.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit</automated>
<fails_when>tsc returns non-zero error</fails_when>
</verify>
</task>

<task id="06-04-02" wave="4">
<title>Баннер ZeroBalanceAlert и блокировка отправки при нулевом балансе</title>
<action>
1. Создать `frontend/src/components/chat/ZeroBalanceAlert.tsx`:
   - Принимает коллбэк `onOpenReferral: () => void`.
   - Предупреждающий баннер в стиле Modern AI Dark: фон `bg-amber-950/40 border border-amber-500/30 text-amber-200`, иконка `AlertTriangle`.
   - Текст: "Баланс исчерпан (0.00 ₽). Пополните баланс или пригласите друзей в реферальную программу для получения +2.00 ₽ за каждого.".
   - Кнопка действия "Получить бонус" (иконка `Gift`), вызывающая `onOpenReferral()`.
2. В `frontend/src/components/chat/ChatInput.tsx`:
   - Проверять условие `isZeroBalance = !user || user.balance_rub <= 0`.
   - Если `isZeroBalance === true`:
     - Текстовое поле переводится в `disabled` с плейсхолдером "Баланс израсходован. Пригласите друзей, чтобы продолжить диалог.".
     - Кнопка отправки блокируется `disabled`.
     - Непосредственно над полем ввода рендерится `ZeroBalanceAlert`.
   - Если при стриминге от сервера получен статус HTTP 402 Payment Required — устанавливать локальный баланс в 0 и показывать блокировку.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit</automated>
<fails_when>tsc returns non-zero error</fails_when>
</verify>
</task>

<task id="06-04-03" wave="4">
<title>Модальное окно ReferralModal и сквозная валидация сборки</title>
<action>
1. Создать `frontend/src/components/modals/ReferralModal.tsx`:
   - Считывает `isReferralModalOpen`, `setReferralModalOpen`, `user` из `useChatStore`.
   - Если `!isReferralModalOpen`, возвращает `null`.
   - Модальное окно с центрированием, полупрозрачным backdrop (`bg-black/70 backdrop-blur-sm`) и анимацией появления.
   - Заголовок "Реферальная программа" с иконкой `Gift` и кнопкой закрытия `X`.
   - Информационный блок: "Приглашайте друзей и получайте по +2.00 ₽ на баланс за каждого зарегистрированного пользователя!".
   - Поле реферальной ссылки: `https://<domain>/?ref=<user.referral_code>` (с fallback на `window.location.origin`).
   - Кнопка "Копировать": копирует ссылку в `navigator.clipboard.writeText`, временно меняет текст на "✓ Скопировано!".
   - Блок статистики: карточка "Приглашено друзей" (`user?.invited_count ?? 0`) и карточка "Заработано бонусов" (`formatRub(user?.referral_earnings_rub ?? 0)`).
2. Подключить `ReferralModal` в `frontend/src/app/chat/layout.tsx`.
3. Запустить полную проверку типов и production-сборку:
   ```bash
   cd frontend && npx tsc --noEmit && npm run build
   ```
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit && npm run build</automated>
<fails_when>tsc or next build returns non-zero exit code</fails_when>
</verify>
</task>

</tasks>
