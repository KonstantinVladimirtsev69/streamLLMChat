---
phase: 06-next-js-frontend-interface
plan: 02
wave: 2
depends_on:
  - "06-01"
requirements:
  - R2
  - UI-02
files_modified:
  - frontend/src/components/sidebar/ChatItem.tsx
  - frontend/src/components/sidebar/ChatSidebar.tsx
  - frontend/src/components/sidebar/UserProfileBar.tsx
  - frontend/src/components/sidebar/MobileDrawer.tsx
  - frontend/src/app/chat/layout.tsx
autonomous: true
must_haves:
  truths:
    - "D-01: Боковая панель шириной 260px отображает список диалогов с сортировкой по дате обновления"
    - "Пользователь может создать новый диалог (POST /api/v1/chats) и удалить существующий (DELETE /api/v1/chats/{id})"
    - "D-12: Компоненты сайдбара декомпозированы с соблюдением лимита 200–250 строк на файл"
    - "На экранах <768px сайдбар трансформируется в адаптивный выезжающий drawer с кнопкой-гамбургером и backdrop"
  artifacts:
    - path: "frontend/src/components/sidebar/ChatSidebar.tsx"
      provides: "Основной компонент боковой панели диалогов"
      contains: "ChatSidebar"
    - path: "frontend/src/components/sidebar/ChatItem.tsx"
      provides: "Элемент списка диалогов с кнопкой удаления"
      contains: "ChatItem"
    - path: "frontend/src/components/sidebar/UserProfileBar.tsx"
      provides: "Блок профиля пользователя и логаута"
      contains: "UserProfileBar"
    - path: "frontend/src/components/sidebar/MobileDrawer.tsx"
      provides: "Мобильная шторка сайдбара с backdrop"
      contains: "MobileDrawer"
  key_links:
    - from: "frontend/src/components/sidebar/ChatSidebar.tsx"
      to: "/api/v1/chats"
      via: "apiFetch GET and POST"
      pattern: "/api/v1/chats"
    - from: "frontend/src/components/sidebar/UserProfileBar.tsx"
      to: "/api/v1/auth/logout"
      via: "apiFetch POST"
      pattern: "/api/v1/auth/logout"
  prohibitions:
    - statement: "MUST NOT mix sidebar UI logic and message streaming in the same component"
      status: resolved
      verification: code_review
    - statement: "MUST NOT exceed 250 lines in any component file"
      status: resolved
      verification: code_review
---

<objective>
Реализовать боковую панель управления сессиями диалогов (R2, UI-02): компонент `ChatItem` с активным состоянием и подтверждением удаления, компонент `ChatSidebar` с загрузкой списка диалогов (`GET /api/v1/chats`) и созданием нового чата (`POST /api/v1/chats`), нижний блок профиля `UserProfileBar` с аватаром и выходом (`POST /api/v1/auth/logout`), а также адаптивную выезжающую шторку `MobileDrawer` для экранов `<768px` с оверлеем и кнопкой-гамбургером.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-next-js-frontend-interface/06-SPEC.md
@.planning/phases/06-next-js-frontend-interface/06-CONTEXT.md
@.planning/phases/06-next-js-frontend-interface/06-RESEARCH.md
@.planning/phases/06-next-js-frontend-interface/06-VALIDATION.md
@.planning/sketches/001-chat-workspace/index.html
</context>

<tasks>

<task id="06-02-01" wave="2">
<title>Компонент ChatItem с активным состоянием и удалением диалога</title>
<action>
1. Создать `frontend/src/components/sidebar/ChatItem.tsx`:
   - Принимает пропсы: `chat: Chat`, `isActive: boolean`, `onSelect: () => void`, `onDelete: () => void`.
   - Отображает заголовок диалога (сокращение длинных названий через `truncate`), иконку сообщения `MessageSquare` из `lucide-react`, дату/время последнего обновления.
   - Стили Modern AI Dark: активный элемент выделяется фоном `bg-[#1a1f36]` с левой синей полосой-акцентом или границей; при наведении (hover) плавное затемнение.
   - Кнопка удаления (иконка `Trash2`): появляется при наведении (или всегда на тач-устройствах), клик вызывает всплывающее подтверждение ("Удалить диалог?") во избежание случайной потери данных.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit</automated>
<fails_when>tsc returns non-zero error</fails_when>
</verify>
</task>

<task id="06-02-02" wave="2">
<title>Компоненты ChatSidebar и UserProfileBar с интеграцией REST API</title>
<action>
1. Создать `frontend/src/components/sidebar/UserProfileBar.tsx`:
   - Получает пользователя из `useChatStore`.
   - Отображает аватар (или плейсхолдер с первой буквой имени), имя пользователя.
   - Кнопка выхода с иконкой `LogOut`: выполняет `apiFetch('/api/v1/auth/logout', { method: 'POST' })`, сбрасывает состояние стора `resetState()` и перенаправляет на `/`.
2. Создать `frontend/src/components/sidebar/ChatSidebar.tsx`:
   - Верхняя кнопка «Новый диалог» (`Plus` иконка): вызывает `apiFetch('/api/v1/chats', { method: 'POST', body: JSON.stringify({ model: selectedModel }) })`, добавляет созданный чат в стор и делает его активным.
   - Список диалогов: загружает `GET /api/v1/chats` при монтировании, рендерит список `ChatItem`, сортируя по `updated_at` (новые сверху).
   - Обработка удаления чата через `apiFetch('/api/v1/chats/' + id, { method: 'DELETE' })`: удаляет чат из списка в сторе; если удален активный — переключает на первый оставшийся или сбрасывает в null.
   - Внизу панели встраивает `UserProfileBar`.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit</automated>
<fails_when>tsc returns non-zero error</fails_when>
</verify>
</task>

<task id="06-02-03" wave="2">
<title>Адаптивная выезжающая шторка MobileDrawer и лэйаут /chat</title>
<action>
1. Создать `frontend/src/components/sidebar/MobileDrawer.tsx`:
   - Использует `isMobileDrawerOpen` и `setMobileDrawerOpen` из `useChatStore`.
   - На экранах `<768px` рендерит выезжающий сайдбар поверх контента с плавным `transform transition-transform duration-300`.
   - Полупрозрачный фон-backdrop (`bg-black/60 backdrop-blur-xs`), клик по которому закрывает drawer.
   - Закрытие шторки при клике на выбор любого чата или кнопку «Новый диалог».
2. Создать/обновить `frontend/src/app/chat/layout.tsx`:
   - Двухколоночный лэйаут для десктопа: фиксированный десктопный `ChatSidebar` (ширина 260px, скрыт на экранах `<md:`), основная рабочая область `flex-1`.
   - Подключение `MobileDrawer` для мобильных экранов.
   - Проверка профиля: вызов `GET /api/v1/auth/me` при старте для гидрации данных пользователя и баланса в сторе.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit</automated>
<fails_when>tsc returns non-zero error</fails_when>
</verify>
</task>

</tasks>
