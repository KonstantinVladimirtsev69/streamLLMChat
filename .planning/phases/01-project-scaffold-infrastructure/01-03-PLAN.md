---
phase: 01-project-scaffold-infrastructure
plan: 03
wave: 2
depends_on:
  - "01-01"
  - "01-02"
requirements:
  - INFRA-02
  - INFRA-04
files_modified:
  - backend/Dockerfile
  - frontend/Dockerfile
  - Makefile
autonomous: true
must_haves:
  truths:
    - "backend/Dockerfile and frontend/Dockerfile build self-contained images without external dependency"
    - "Makefile exposes documented targets for build, run, test, docker-up, docker-down, and clean"
  artifacts:
    - path: "backend/Dockerfile"
      provides: "Go backend multi-stage container"
      contains: "FROM golang"
    - path: "frontend/Dockerfile"
      provides: "Next.js frontend multi-stage container"
      contains: "FROM node"
    - path: "Makefile"
      provides: "Project development tasks runner"
      contains: "docker-up:"
  key_links:
    - from: "Makefile"
      to: "docker-compose.yml"
      via: "compose invocation in docker-up"
      pattern: "docker compose up"
  prohibitions:
    - statement: "MUST NOT создавать общие зависимости между Dockerfile backend и frontend (изолированная сборка в Dokploy)"
      status: resolved
      verification: test
---

<objective>
Создать независимые многоэтапные `Dockerfile` для `backend` и `frontend`, оптимизированные для деплоя в Dokploy, и корневой `Makefile` для автоматизации сборки, тестирования и управления контейнерами.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffold-infrastructure/01-SPEC.md
@.planning/phases/01-project-scaffold-infrastructure/01-01-PLAN.md
@.planning/phases/01-project-scaffold-infrastructure/01-02-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Создание Dockerfile для backend и frontend</name>
  <files>backend/Dockerfile, frontend/Dockerfile</files>
  <read_first>backend/go.mod, frontend/package.json, frontend/next.config.ts, .planning/phases/01-project-scaffold-infrastructure/01-SPEC.md</read_first>
  <action>
    Создать `backend/Dockerfile`:
    - Stage 1 (builder): `FROM golang:1.27-alpine AS builder`, установка git/ca-certificates, копирование `go.mod` и `go.sum`, запуск `go mod download`, копирование исходного кода, компиляция `CGO_ENABLED=0 GOOS=linux go build -ldflags="-w -s" -o /app/server ./main.go`.
    - Stage 2 (runner): `FROM alpine:3.21 AS runner`, создание непривилегированного пользователя appuser, копирование `/app/server` из builder, установка USER appuser, EXPOSE 8080, CMD ["/app/server"].
    - Контекст сборки должен быть строго изолированным для папки `backend/`.

    Создать `frontend/Dockerfile`:
    - Stage 1 (deps): `FROM node:22-alpine AS deps`, копирование `package.json` и `package-lock.json`, запуск `npm ci`.
    - Stage 2 (builder): `FROM node:22-alpine AS builder`, копирование node_modules и исходного кода, запуск `npm run build`.
    - Stage 3 (runner): `FROM node:22-alpine AS runner`, создание пользователя nextjs, копирование `public`, `.next/standalone` и `.next/static`, EXPOSE 3000, ENV PORT=3000, CMD ["node", "server.js"].
    - Контекст сборки строго изолирован для папки `frontend/`.
  </action>
  <acceptance_criteria>
    - backend/Dockerfile содержит multi-stage инструкции "AS builder" и "AS runner"
    - frontend/Dockerfile содержит multi-stage сборку с standalone сервером
    - `test -f backend/Dockerfile && test -f frontend/Dockerfile` завершается с кодом 0
  </acceptance_criteria>
  <verify>
    <automated>test -f backend/Dockerfile && test -f frontend/Dockerfile</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <done>Многоэтапные Dockerfile для backend и frontend созданы с поддержкой standalone и непривилегированных пользователей</done>
</task>

<task type="auto">
  <name>Task 2: Создание корневого Makefile</name>
  <files>Makefile</files>
  <read_first>docker-compose.yml, .planning/phases/01-project-scaffold-infrastructure/01-SPEC.md</read_first>
  <action>
    Создать корневой `Makefile` со следующими целями:
    - `.DEFAULT_GOAL := help`
    - `help`: вывод цветного форматированного списка доступных команд.
    - `build`: последовательный запуск сборки `go -C backend build -o bin/server ./...` и `npm --prefix frontend run build`.
    - `run-backend`: запуск `go -C backend run ./main.go`.
    - `run-frontend`: запуск `npm --prefix frontend run dev`.
    - `test`: запуск `go -C backend test -v ./...` и `npm --prefix frontend run lint` (если скрипт присутствует).
    - `docker-up`: `docker compose up -d`.
    - `docker-down`: `docker compose down`.
    - `docker-build`: сборка локальных образов `docker build -t llm-chat-backend:latest backend/` и `docker build -t llm-chat-frontend:latest frontend/`.
    - `clean`: удаление `backend/bin/`, временных файлов и кэшей.
  </action>
  <acceptance_criteria>
    - Makefile содержит цели help, build, run-backend, run-frontend, test, docker-up, docker-down, docker-build, clean
    - `make -n help` успешно отрабатывает без синтаксических ошибок make
  </acceptance_criteria>
  <verify>
    <automated>make -n help</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <done>Makefile успешно парсится утилитой make и содержит все требуемые цели</done>
</task>
</tasks>

<verification>
- [ ] `make -n help` отрабатывает без синтаксических ошибок Makefile
- [ ] `backend/Dockerfile` и `frontend/Dockerfile` содержат корректные инструкции многоэтапной сборки
</verification>

<success_criteria>
- Создан корневой `Makefile` с целями `help`, `build`, `run-*`, `test`, `docker-up`, `docker-down`, `clean`
- Созданы автономные `Dockerfile` для `backend` и `frontend`, готовые для деплоя в Dokploy
</success_criteria>

## Artifacts this phase produces

- `backend/Dockerfile`: автономный образ Go сервиса
- `frontend/Dockerfile`: автономный образ Next.js сервиса
- `Makefile`: автоматизация команд сборки, запуска и контейнеров

<output>
После завершения создать `.planning/phases/01-project-scaffold-infrastructure/01-03-SUMMARY.md`
</output>
