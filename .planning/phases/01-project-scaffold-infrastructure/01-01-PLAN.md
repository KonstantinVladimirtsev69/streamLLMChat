---
phase: 01-project-scaffold-infrastructure
plan: 01
wave: 1
depends_on: []
requirements:
  - INFRA-01
files_modified:
  - backend/go.mod
  - backend/go.sum
  - backend/main.go
  - backend/internal/server/server.go
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/next.config.ts
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/globals.css
autonomous: true
must_haves:
  truths:
    - "Backend Go module initializes with Go 1.27+ and serves GET /health returning status ok"
    - "Frontend Next.js 16 app compiles cleanly with TypeScript and TailwindCSS"
  artifacts:
    - path: "backend/go.mod"
      provides: "Go module definition"
      contains: "module backend"
    - path: "backend/main.go"
      provides: "Backend entry point"
      contains: "chi.NewRouter"
    - path: "frontend/package.json"
      provides: "Next.js frontend dependencies"
      contains: "next"
    - path: "frontend/src/app/page.tsx"
      provides: "Frontend entry page"
      contains: "export default function"
  key_links:
    - from: "backend/main.go"
      to: "backend/internal/server/server.go"
      via: "server initialization and routing"
      pattern: "server\\.New"
  prohibitions:
    - statement: "MUST NOT использовать версии Go ниже 1.27 или Next.js ниже 16"
      status: resolved
      verification: test
---

<objective>
Инициализировать каркас монорепозитория: Go 1.27+ сервис в папке `backend/` с роутером `chi/v5` и эндпоинтом проверки жизнеспособности `GET /health`, а также приложение Next.js 16+ в папке `frontend/` с App Router, TypeScript и TailwindCSS.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffold-infrastructure/01-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Инициализация Go бэкенда с chi/v5 и GET /health</name>
  <files>backend/go.mod, backend/main.go, backend/internal/server/server.go</files>
  <read_first>.planning/phases/01-project-scaffold-infrastructure/01-SPEC.md</read_first>
  <action>
    Создать директорию backend.
    Инициализировать Go модуль с именем `backend` и версией go 1.27.
    Подключить `github.com/go-chi/chi/v5` и `github.com/go-chi/cors`.
    Создать `backend/internal/server/server.go` с экспортируемой структурой `Server` и функцией `New() *Server`, настраивающей роутер chi с middleware Logger, Recoverer, CORS и обработчиком `r.Get("/health", ...)` отдающим JSON `{"status":"ok"}`.
    Создать `backend/main.go`, инициализирующий `server.New()`, считывающий переменную окружения `PORT` (по умолчанию 8080) и запускающий `http.ListenAndServe` с graceful shutdown через `os.Signal` (SIGINT, SIGTERM).
  </action>
  <acceptance_criteria>
    - backend/go.mod содержит "module backend" и директиву версии "go 1.27"
    - backend/internal/server/server.go содержит "chi.NewRouter()" и маршрут "/health"
    - `go -C backend build -o /dev/null ./...` завершается с кодом 0
  </acceptance_criteria>
  <verify>
    <automated>go -C backend build -o /dev/null ./...</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <done>Бэкенд на Go 1.27+ успешно компилируется, содержит chi роутер и эндпоинт /health</done>
</task>

<task type="auto">
  <name>Task 2: Инициализация Next.js 16 фронтенда</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/next.config.ts, frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/src/app/globals.css</files>
  <read_first>.planning/phases/01-project-scaffold-infrastructure/01-SPEC.md</read_first>
  <action>
    Создать директорию `frontend/`.
    Создать `frontend/package.json` с зависимостями: `next`: "^16.0.0", `react`: "^19.0.0", `react-dom`: "^19.0.0", `@types/react`, `@types/node`, `typescript`, `tailwindcss`, `postcss`, `autoprefixer`.
    Настроить `frontend/next.config.ts` с директивой `output: "standalone"`.
    Создать `frontend/tsconfig.json` со стандартной строгой конфигурацией Next.js и path alias `@/*` на `./src/*`.
    Создать `frontend/src/app/layout.tsx` с подключением шрифтов, метаданных и `globals.css`.
    Создать `frontend/src/app/page.tsx` с карточкой приветствия "LLM Chat Platform", индикатором готовности сервиса и ссылкой на статус бэкенда.
    Создать `frontend/src/app/globals.css` с директивами TailwindCSS.
    Установить зависимости с помощью `npm --prefix frontend install`.
  </action>
  <acceptance_criteria>
    - frontend/package.json содержит `"next": "^16` или выше
    - frontend/next.config.ts содержит `output: "standalone"`
    - `npm --prefix frontend run build` успешно компилирует standalone сборку без ошибок
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <done>Next.js 16 проект с TypeScript и TailwindCSS успешно собирается без ошибок</done>
</task>
</tasks>

<verification>
- [ ] `go -C backend build ./...` компилирует сервис без ошибок
- [ ] `npm --prefix frontend run build` успешно выполняет сборку Next.js приложения
</verification>

<success_criteria>
- В монорепозитории существуют директории `backend/` и `frontend/`
- Сервис `backend/` написан на Go 1.27+ с использованием `chi/v5` и эндпоинта `GET /health`
- Приложение `frontend/` собрано на Next.js 16+ с TypeScript и TailwindCSS в режиме standalone
</success_criteria>

## Artifacts this phase produces

- `backend/go.mod`: файл модуля Go 1.27
- `backend/main.go`: точка входа сервера
- `backend/internal/server/server.go`: структура `Server` и роутер chi
- `frontend/package.json`: манифест зависимостей Next.js 16
- `frontend/next.config.ts`: конфигурация Next.js с output standalone
- `frontend/src/app/layout.tsx`: корневой макет
- `frontend/src/app/page.tsx`: главная страница

<output>
После завершения создать `.planning/phases/01-project-scaffold-infrastructure/01-01-SUMMARY.md`
</output>
