---
phase: 02-database-layer-data-models
plan: 01
wave: 1
depends_on: []
requirements:
  - DATA-01
files_modified:
  - backend/go.mod
  - backend/go.sum
  - backend/migrations/000001_init_schema.up.sql
  - backend/migrations/000001_init_schema.down.sql
  - backend/internal/model/user.go
  - backend/internal/model/balance.go
  - backend/internal/model/transaction.go
  - backend/internal/model/referral.go
  - backend/internal/database/migrate.go
  - Makefile
autonomous: true
must_haves:
  truths:
    - "PostgreSQL migrations apply and roll back cleanly using golang-migrate"
    - "Tables users, balances, balance_transactions, referrals created with constraints and CHECK (amount_kopecks >= 0)"
    - "Domain models define Go structures for relational entities with strict kopeck types"
  artifacts:
    - path: "backend/migrations/000001_init_schema.up.sql"
      provides: "PostgreSQL DDL schema"
      contains: "CREATE TABLE IF NOT EXISTS users"
    - path: "backend/migrations/000001_init_schema.down.sql"
      provides: "PostgreSQL rollback DDL"
      contains: "DROP TABLE IF EXISTS"
    - path: "backend/internal/model/balance.go"
      provides: "Balance domain model"
      contains: "AmountKopecks"
    - path: "backend/internal/database/migrate.go"
      provides: "Embedded migration runner"
      contains: "go:embed"
  key_links:
    - from: "backend/internal/database/migrate.go"
      to: "backend/migrations/000001_init_schema.up.sql"
      via: "embed filesystem"
      pattern: "migrations"
  prohibitions:
    - statement: "MUST NOT хранить баланс в float или рублях с плавающей точкой — строго BIGINT копейки"
      status: resolved
      verification: code
---

<objective>
Создать схемы и миграции PostgreSQL для пользователей, балансов, истории транзакций и рефералов, определить Go-модели сущностей и реализовать встроенный запуск миграций через `golang-migrate` с поддержкой команд в Makefile.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-database-layer-data-models/02-SPEC.md
@.planning/phases/02-database-layer-data-models/02-CONTEXT.md
@.planning/phases/02-database-layer-data-models/02-RESEARCH.md
</context>

<tasks>

<task id="02-01-01" wave="1">
<title>Подключение зависимостей pgx/v5 и golang-migrate</title>
<instruction>
1. Добавить в `backend/go.mod` зависимости `github.com/jackc/pgx/v5` и `github.com/golang-migrate/migrate/v4` (с драйверами `database/pgx/v5` и `source/iofs`).
2. Выполнить `go mod tidy` в `backend/`.
</instruction>
<verify>
<automated>go -C backend list -m github.com/jackc/pgx/v5 github.com/golang-migrate/migrate/v4</automated>
</verify>
</task>

<task id="02-01-02" wave="1">
<title>Создание SQL миграций схемы PostgreSQL</title>
<instruction>
1. Создать `backend/migrations/000001_init_schema.up.sql`:
   - Таблица `users`: `id` (BIGSERIAL PRIMARY KEY), `vk_id` (BIGINT UNIQUE NOT NULL), `first_name`, `last_name`, `avatar_url`, `ref_code` (VARCHAR(32) UNIQUE NOT NULL), `referred_by` (BIGINT REFERENCES users(id) ON DELETE SET NULL), `created_at`, `updated_at`.
   - Таблица `balances`: `user_id` (BIGINT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE), `amount_kopecks` (BIGINT NOT NULL DEFAULT 0 CHECK (amount_kopecks >= 0)), `updated_at`.
   - Таблица `balance_transactions`: `id` (BIGSERIAL PRIMARY KEY), `user_id` (BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE), `amount_kopecks` (BIGINT NOT NULL), `balance_after_kopecks` (BIGINT NOT NULL), `type` (VARCHAR(32) NOT NULL CHECK (type IN ('welcome_bonus', 'referral_reward', 'token_charge', 'deposit', 'adjustment'))), `reference_id` (VARCHAR(64)), `description` (TEXT NOT NULL DEFAULT ''), `created_at`.
   - Таблица `referrals`: `id` (BIGSERIAL PRIMARY KEY), `referrer_id` (BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE), `referee_id` (BIGINT NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE), `reward_kopecks` (BIGINT NOT NULL DEFAULT 200), `created_at`, `CHECK (referrer_id <> referee_id)`.
   - Индексы: `idx_users_vk_id`, `idx_users_ref_code`, `idx_transactions_user_id`, `idx_referrals_referrer_id`.
2. Создать `backend/migrations/000001_init_schema.down.sql` с корректным порядком удаления таблиц и индексов.
</instruction>
<verify>
<automated>test -f backend/migrations/000001_init_schema.up.sql && test -f backend/migrations/000001_init_schema.down.sql</automated>
</verify>
</task>

<task id="02-01-03" wave="1">
<title>Создание Go доменных моделей сущностей PostgreSQL</title>
<instruction>
1. В `backend/internal/model/` создать:
   - `user.go`: структура `User` с JSON-тегами и полями `ID`, `VKID`, `FirstName`, `LastName`, `AvatarURL`, `RefCode`, `ReferredBy`, `CreatedAt`, `UpdatedAt`.
   - `balance.go`: структура `Balance` (`UserID`, `AmountKopecks`, `UpdatedAt`) и константы (`WelcomeBonusKopecks = 500`, `ReferralRewardKopecks = 200`).
   - `transaction.go`: структура `BalanceTransaction` (`ID`, `UserID`, `AmountKopecks`, `BalanceAfterKopecks`, `Type`, `ReferenceID`, `Description`, `CreatedAt`) и константы типов транзакций.
   - `referral.go`: структура `Referral` (`ID`, `ReferrerID`, `RefereeID`, `RewardKopecks`, `CreatedAt`).
   - `errors.go`: доменные ошибки `ErrNotFound`, `ErrInsufficientBalance`, `ErrUserAlreadyExists`.
</instruction>
<verify>
<automated>go -C backend test ./internal/model/...</automated>
</verify>
</task>

<task id="02-01-04" wave="1">
<title>Реализация встроенного раннера миграций и целей в Makefile</title>
<instruction>
1. Создать `backend/internal/database/migrate.go` с директивой `//go:embed ../../migrations/*.sql` и функциями `Up(databaseURL string) error` и `Down(databaseURL string) error` с использованием `golang-migrate/migrate/v4/source/iofs`.
2. Добавить цели `migrate-up` и `migrate-down` в корневой `Makefile`.
</instruction>
<verify>
<automated>go -C backend build ./internal/database/... && make -n migrate-up</automated>
</verify>
</task>

</tasks>
